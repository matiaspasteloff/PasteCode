{
  "$schema": "https://unpkg.com/knip@6/schema.json",
  "workspaces": {
    ".": {
      // La compuerta de contraste de RNF-22. No la importa nadie: la corre
      // `pnpm check` por ruta, que es lo que la hace un script y no un módulo.
      "entry": ["scripts/*.mjs"],
    },
    "apps/desktop": {
      // El shell falso del test del supervisor no lo importa nadie: se lo pasa
      // como ruta a `spawn`, que es justamente lo que lo hace un binario y no
      // un módulo. Knip no puede verlo y lo reportaría como muerto.
      "entry": ["src/renderer/main.tsx", "scripts/*.mjs", "src/main/**/fake-*.mjs"],
      // El servidor de TypeScript no se importa: se lo lanza como proceso hijo
      // pasándole la ruta de su `cli.mjs`, que es justamente lo que lo hace un
      // ejecutable y no una librería. `@vscode/ripgrep` no está acá porque ése
      // sí se importa —para preguntarle dónde quedó su binario—, y ésa es toda
      // la diferencia.
      "ignoreDependencies": ["typescript-language-server"],
    },
    "extensions/word-count": {
      // Una extensión no la importa nadie: el host la carga con un `import()`
      // dinámico armado desde el `main` de su manifest, que es justamente lo
      // que la hace una extensión y no un módulo del IDE. Knip no puede ver esa
      // arista y reportaría el punto de entrada como muerto, y de arrastre
      // reportaría `@pastecode/extension-api` como dependencia sin usar.
      "entry": ["src/extension.ts"],
    },
    "e2e": {
      "entry": ["tests/**/*.spec.ts"],
      // El E2E no importa nada de @pastecode/desktop: lanza la app por ruta.
      // La dependencia igual es real y hace falta, porque es lo que le dice a
      // Turborepo que tiene que buildear la app antes de correr los tests
      // (el `dependsOn: ["^build"]` de turbo.json). Sacarla haría que el E2E
      // corriera contra el `out/` de la corrida anterior, o contra nada.
      "ignoreDependencies": ["@pastecode/desktop"],
    },
  },
}
